<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/dark_bg">

    <ListView
        android:id="@+id/list_view_quotes"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@string/current_stock_listing"
        android:padding="@dimen/padding_small" />

    <TextView
        android:id="@+id/text_view_stock_unavailable"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:contentDescription="@string/stocks_unavailable_error_widget"
        android:drawablePadding="@dimen/padding_small"
        android:drawableTop="@drawable/ic_refresh_white_24dp"
        android:gravity="center"
        android:padding="@dimen/padding_avg"
        android:text="@string/stocks_unavailable_error_widget"
        android:textSize="@dimen/text_size_small"
        android:visibility="invisible" />
</RelativeLayout>